# DeepSeek API key（必填，用于生成文章）
# 在 https://platform.deepseek.com 创建
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx

# 可选：指定成文模型（默认 deepseek-flash）
# 可选值：deepseek-flash（快） / deepseek-v4-pro（慢约 4 倍，适合重要节目）
# DEEPSEEK_MODEL=deepseek-flash

# 说明：DeepSeek 的思考模式默认开启，但本项目全程关闭 thinking
# （extra_body={"thinking": {"type": "disabled"}}）。原因见 README「模型与思考模式」一节：
# 长上下文下思考量会膨胀到 5000+ 字并吃光 token 预算，导致正文为空；
# 且思考模式下 temperature 不生效，会破坏逐节写作的篇幅控制。

# Notion 集成（用于把文章写入你的 Notion）
# 1. https://www.notion.so/profile/integrations 创建 Internal Integration，复制 Secret
# 2. 在 Notion 里打开目标数据库或页面 → 连接 → 添加你创建的 integration
NOTION_TOKEN=
# 二选一：写入数据库（作为一行）或父页面（作为子页面）；都填优先数据库
# NOTION_DATABASE_ID=32位或36位id
# NOTION_PARENT_PAGE_ID=32位或36位id
